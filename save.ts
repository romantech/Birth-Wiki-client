      {/* swiper_Modal */}
      {/* {showCard ? (
        <CardBg ref={cardRef} onClick={closeCard}>
          <animated.div style={animation}>
            <SwiperCard issue={issue} music={music} movie={movie} birth={birth} death={death} />
            {<CoverFlow issue={issue} music={music} movie={movie} birth={birth} death={death} />}
          </animated.div>
        </CardBg>
      ) : null} */}
      {/* swiper_Modal */}